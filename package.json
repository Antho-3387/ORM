{
  "name": "magic-decks",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd orm && npm run dev",
    "build": "cd orm && npm run build",
    "start": "cd orm && npm run start",
    "lint": "cd orm && npm run lint"
  }
}
