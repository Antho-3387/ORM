{
  "name": "magic-decks",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd orm && npm run dev",
    "build": "echo 'Installing root deps...' && npm install && echo 'Installing orm deps...' && cd orm && npm install && echo 'Building project...' && npm run build",
    "start": "cd orm && npm run start",
    "lint": "cd orm && npm run lint"
  },
  "dependencies": {
    "@prisma/client": "^7.6.0"
  },
  "devDependencies": {
    "prisma": "^7.6.0"
  }
}
